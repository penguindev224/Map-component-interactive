import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // App.js where the navigation and routing happens
import './index.css'; // Global styles

ReactDOM.render(<App />, document.getElementById('root'));